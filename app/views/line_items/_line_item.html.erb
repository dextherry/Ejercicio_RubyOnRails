<% if line_item == @current_line %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
<td class="total_cell"><%= button_to 'v', {:controller => line_items_path, :action => :decrease, :id => line_item.id}, :remote => true %>
</td>
	<td><%= line_item.quantity %>&times;</td>
	<td><%= line_item.product.title %></td>
	<td class="item_price"><%= number_to_currency(line_item.total_price) %></td>

</tr>